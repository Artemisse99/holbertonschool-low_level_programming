#include "main.h"

/**
 * _isdigit - print if _isdigit
 *
 * @c: The character to print
 *
 * Return: using two
 */
int _isdigit(int c)
{
	if (c >= 48 && c <= 57)
	{
		return (1);
	}

	else

	{
		return (0);
	}
}
