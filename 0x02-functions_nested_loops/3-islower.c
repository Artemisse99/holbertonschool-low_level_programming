#include "main.h"

/**
 * _islower - print if lowercase
 *
 * @c: The character to print
 *
 * Return: using two
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (1);
	}

	else

	{
		return (0);
	}
}
