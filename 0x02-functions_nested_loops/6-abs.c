#include "main.h"

/**
 * _abs - print if lowercase
 *
 * @x: The character to print
 *
 * Return: using two
 */
int _abs(int x)
{

	if (x > 0)
	{
	return (x);
	}
	else
	{
		return (x * -1);
	}
	_putchar('\n');
}
